services:
  - type: web
    name: vehicle-ocr-backend
    runtime: python
    plan: free
    rootDir: be
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
